[package]
name = "serdde-rust-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ciborium = "0.2.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
debug = false
lto = false
